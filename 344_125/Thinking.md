# Complexities -->

* In Both the solution I tried optimal way to solve the problem in their own way.
* The **Time** was **O(n)**  as I have iterated over the string and list.
* In Valid Palindrom the **Space** was **O(n)** as I have made another string also can solve the problem using only **O(1)** by first lowercase the input string and compare the input and against it by replacing in place
* In Reverse String **Space** was **O(1)** as question was around it !


# Thinking !!!

> In First question I use build in operator and two pointer
> and For loop and another string in second question !
